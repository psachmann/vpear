using Fluxor;
using System.Threading.Tasks;

/*
public class ShowPopupEffect : Effect<ShowPopupAction>
{
    private readonly NavigationService _navigationService;

    public ShowPopupEffect(NavigationService navigationService)
    {
        _navigationService = navigationService;
    }

    public override Task HandleAsync(ShowPopupAction action, IDispatcher dispatcher)
    {
        _navigationService.ShowPopup();

        return Task.CompletedTask;
    }
}

public class ClosePopupEffect : Effect<ClosePopupAction>
{
    private readonly NavigationService _navigationService;

    public ClosePopupEffect(NavigationService navigationService)
    {
        _navigationService = navigationService;
    }

    public override Task HandleAsync(ClosePopupAction action, IDispatcher dispatcher)
    {
        _navigationService.ClosePopup();

        return Task.CompletedTask;
    }
}
*/
